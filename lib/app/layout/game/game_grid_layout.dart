import 'package:flutter/material.dart';

class GameGridLayout extends StatelessWidget {
  final Widget content;
  const GameGridLayout({required this.content, Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return content;
  }
}
